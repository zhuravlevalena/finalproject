# AI-Ассистент для создания карточек товаров

AI-ассистент, который помогает продавцу маркетплейса пройти путь от фотографии товара до готовой карточки, полностью соответствующей требованиям площадки.

## Возможности

- 📸 Загрузка фотографий товаров
- 🎨 Генерация изображений с помощью DALL·E 3
- 🛍️ Поддержка различных маркетплейсов (Wildberries, Ozon, Яндекс.Маркет)
- 📝 Редактор карточек с Fabric.js
- 🎯 AI-профили товаров (запоминание стиля, типа товара, целевой аудитории)
- 📋 Шаблоны карточек для разных маркетплейсов

## Технологии

### Backend
- Node.js + Express
- Sequelize (PostgreSQL)
- OpenAI API (DALL·E 3)
- Multer (загрузка файлов)
- JWT аутентификация

### Frontend
- React + TypeScript
- Redux Toolkit
- Fabric.js (редактор Canvas)
- TanStack Query
- Tailwind CSS
- Wouter (роутинг)

## Установка

### Требования
- Node.js 18+
- PostgreSQL
- OpenAI API ключ

### Backend

1. Перейдите в директорию `server`:
```bash
cd server
```

2. Установите зависимости:
```bash
npm install
```

3. Создайте файл `.env` в корне `server`:
```env
PORT=3000
DATABASE_URL=postgres://user:password@localhost:5432/dbname
ACCESS_TOKEN_SECRET=your_access_token_secret
REFRESH_TOKEN_SECRET=your_refresh_token_secret
OPENAI_API_KEY=your_openai_api_key
```

4. Создайте базу данных и выполните миграции:
```bash
npm run db
```

### Frontend

1. Перейдите в директорию `client`:
```bash
cd client
```

2. Установите зависимости:
```bash
npm install
```

## Запуск

### Backend
```bash
cd server
npm run dev
```

Сервер запустится на `http://localhost:3000`

### Frontend
```bash
cd client
npm run dev
```

Клиент запустится на `http://localhost:5173`

## Структура проекта

```
project-your-game/
├── server/              # Backend (Node.js + Express)
│   ├── db/             # База данных (миграции, модели, сидеры)
│   ├── src/
│   │   ├── controller/ # Контроллеры
│   │   ├── services/  # Бизнес-логика
│   │   ├── routes/    # API роуты
│   │   └── middlewares/ # Middleware
│   └── uploads/       # Загруженные файлы
│
└── client/            # Frontend (React + TypeScript)
    └── src/
        ├── entities/   # Сущности (ProductCard, Marketplace, etc.)
        ├── features/   # Фичи (Auth)
        ├── pages/      # Страницы
        ├── widgets/    # Виджеты (CardEditor, Navbar)
        └── shared/     # Общие компоненты и утилиты
```

## API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/refresh` - Обновление токена
- `DELETE /api/auth/logout` - Выход

### Маркетплейсы
- `GET /api/marketplaces` - Список маркетплейсов
- `GET /api/marketplaces/:id` - Получить маркетплейс

### Карточки товаров
- `GET /api/product-cards` - Список карточек пользователя
- `GET /api/product-cards/:id` - Получить карточку
- `POST /api/product-cards` - Создать карточку
- `PUT /api/product-cards/:id` - Обновить карточку
- `DELETE /api/product-cards/:id` - Удалить карточку

### Изображения
- `POST /api/images/upload` - Загрузить изображение
- `POST /api/images/generate` - Сгенерировать изображение (DALL·E)
- `GET /api/images` - Список изображений
- `GET /api/images/:id` - Получить изображение
- `DELETE /api/images/:id` - Удалить изображение

### Шаблоны
- `GET /api/templates` - Список шаблонов
- `GET /api/templates/:id` - Получить шаблон

### Профили товаров
- `GET /api/product-profiles` - Список профилей
- `POST /api/product-profiles/get-or-create` - Создать/получить профиль
- `PUT /api/product-profiles/:id` - Обновить профиль

## Использование

1. Зарегистрируйтесь или войдите в систему
2. Перейдите в "Мои карточки" (Dashboard)
3. Нажмите "Создать карточку"
4. Выберите маркетплейс и шаблон (опционально)
5. Загрузите фото товара или сгенерируйте изображение с помощью AI
6. Отредактируйте карточку в редакторе (добавьте текст, измените элементы)
7. Сохраните карточку

## Лицензия

ISC
